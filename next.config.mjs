/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'fastly.picsum.photos', 'picsum.photos', 'res.cloudinary.com'],
  }
};

export default nextConfig;
